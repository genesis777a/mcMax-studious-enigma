import sys
import argparse

def parse_protocol():
    parser = argparse.ArgumentParser(description='Parse custom protocol commands.')
    parser.add_argument('--protocol', type=str, required=True, help='Protocol name')
    
    args = parser.parse_args()
    
    if args.protocol == "$BS-Project":
        execute_custom_command()
    else:
        print("Unknown protocol: {}".format(args.protocol))

def execute_custom_command():
    print("Error02")

if __name__ == "__main__":
    parse_protocol()
